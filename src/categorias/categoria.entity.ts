// Define una clase llamada 'Categoria'
export class Categoria {
    // Declaración de propiedades de la clase 'Categoria'

    // Propiedad 'id' de tipo string, que generalmente se utiliza para identificar de manera única a una categoria
    id: string

    // Propiedad 'nombre' de tipo string, que almacena el nombre de la categoria
    nombre: string

    // Propiedad 'imagen' de tipo string, que almacena una imagen de la categoría
    imagen: string
}

/** 
 * En resumen, esta clase Categoria define la estructura de datos de una categoría en tu aplicación.
 * Cada propiedad representa un atributo de la categoría, como su nombre, imagen, etc. 
 * Esta clase se utiliza típicamente como una entidad en un sistema, y se puede utilizar para crear, 
 * leer, actualizar y eliminar registros de categorías en una base de datos u otras operaciones relacionadas 
 * con las categorías dentro de la aplicación. 
*/